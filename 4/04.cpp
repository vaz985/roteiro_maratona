#include <bits/stdc++.h>
using namespace std;
int c, f;
vector<pair<int,int> > v;
int main() {
  int testNo = 1;
  while(1) {
    cin >> c >> f;
    if(c==0) break;
    if(testNo>1) cout << endl;
    cout << "Teste " << testNo < <endl;
    v.assing(f);
    for(int i=0; i<f; i++) {
      
    }
  } 
  return 0;
}
